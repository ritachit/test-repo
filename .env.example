# Local dev: SQLite file (default). Production: Turso libsql URL + token.
DATABASE_URL=file:./data/dev.db
DATABASE_AUTH_TOKEN=
# Required in production. Generate with: openssl rand -base64 32
AUTH_SECRET=
